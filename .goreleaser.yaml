# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/zahidcakici/tokenizer-cli/cmd.version={{.Version}}
    binary: tokenizer

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

brews:
  - name: tokenizer
    repository:
      owner: zahidcakici
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/zahidcakici/tokenizer-cli"
    description: "A fast CLI tool to count tokens for various Large Language Model (LLM) providers."
    license: "MIT"
    install: |
      bin.install "tokenizer"
    test: |
      system "#{bin}/tokenizer", "--help"
